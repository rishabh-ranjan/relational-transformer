M3, griffin, harness + inference

M6, 4dbinfer, harness + inference

M1, plurel, inference

M2.1, data scaling, inference

M4, cost + label budget, tabulate on rel-f1

R2-W3, random walk, measure rustler vs forward pass at inference

M2.2, relational vs flat-table, tabulate

R3-W3, compute scaling, tabulate from pretraining curve
